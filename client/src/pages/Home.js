import React from 'react';
import { useQuery } from '@apollo/client';

const Home = () => {

  
    return (
      <main>
        <div className="">
         <h1>Working</h1>
        </div>
      </main>
    );
  };

export default Home;
